// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: wevibe/identity/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "wevibe.identity.v1";

export interface QueryResolveIdentityRequest {
  passkeyPubkey: string;
}

export interface QueryResolveIdentityResponse {
  walletAddress: string;
  isMigrated: boolean;
  found: boolean;
}

function createBaseQueryResolveIdentityRequest(): QueryResolveIdentityRequest {
  return { passkeyPubkey: "" };
}

export const QueryResolveIdentityRequest: MessageFns<QueryResolveIdentityRequest> = {
  encode(message: QueryResolveIdentityRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.passkeyPubkey !== "") {
      writer.uint32(10).string(message.passkeyPubkey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryResolveIdentityRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryResolveIdentityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.passkeyPubkey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryResolveIdentityRequest {
    return {
      passkeyPubkey: isSet(object.passkeyPubkey)
        ? globalThis.String(object.passkeyPubkey)
        : isSet(object.passkey_pubkey)
        ? globalThis.String(object.passkey_pubkey)
        : "",
    };
  },

  toJSON(message: QueryResolveIdentityRequest): unknown {
    const obj: any = {};
    if (message.passkeyPubkey !== "") {
      obj.passkeyPubkey = message.passkeyPubkey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryResolveIdentityRequest>, I>>(base?: I): QueryResolveIdentityRequest {
    return QueryResolveIdentityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryResolveIdentityRequest>, I>>(object: I): QueryResolveIdentityRequest {
    const message = createBaseQueryResolveIdentityRequest();
    message.passkeyPubkey = object.passkeyPubkey ?? "";
    return message;
  },
};

function createBaseQueryResolveIdentityResponse(): QueryResolveIdentityResponse {
  return { walletAddress: "", isMigrated: false, found: false };
}

export const QueryResolveIdentityResponse: MessageFns<QueryResolveIdentityResponse> = {
  encode(message: QueryResolveIdentityResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.walletAddress !== "") {
      writer.uint32(10).string(message.walletAddress);
    }
    if (message.isMigrated !== false) {
      writer.uint32(16).bool(message.isMigrated);
    }
    if (message.found !== false) {
      writer.uint32(24).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryResolveIdentityResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryResolveIdentityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.walletAddress = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isMigrated = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryResolveIdentityResponse {
    return {
      walletAddress: isSet(object.walletAddress)
        ? globalThis.String(object.walletAddress)
        : isSet(object.wallet_address)
        ? globalThis.String(object.wallet_address)
        : "",
      isMigrated: isSet(object.isMigrated)
        ? globalThis.Boolean(object.isMigrated)
        : isSet(object.is_migrated)
        ? globalThis.Boolean(object.is_migrated)
        : false,
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
    };
  },

  toJSON(message: QueryResolveIdentityResponse): unknown {
    const obj: any = {};
    if (message.walletAddress !== "") {
      obj.walletAddress = message.walletAddress;
    }
    if (message.isMigrated !== false) {
      obj.isMigrated = message.isMigrated;
    }
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryResolveIdentityResponse>, I>>(base?: I): QueryResolveIdentityResponse {
    return QueryResolveIdentityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryResolveIdentityResponse>, I>>(object: I): QueryResolveIdentityResponse {
    const message = createBaseQueryResolveIdentityResponse();
    message.walletAddress = object.walletAddress ?? "";
    message.isMigrated = object.isMigrated ?? false;
    message.found = object.found ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
