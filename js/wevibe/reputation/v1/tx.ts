// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: wevibe/reputation/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";

export const protobufPackage = "wevibe.reputation.v1";

export interface MsgUpdateReputation {
  signer: string;
  developer: Uint8Array;
  memoryCid: string;
  difficulty: number;
  quality: number;
  domainTags: string[];
  provenance: string;
}

export interface MsgUpdateReputationResponse {
  xp: string;
}

export interface MsgIncrementContribution {
  authority: string;
  contributorId: string;
  orgId: string;
  memoryCid: string;
}

export interface MsgIncrementContributionResponse {
}

export interface MsgIncrementServe {
  authority: string;
  contributorId: string;
  orgId: string;
  memoryCid: string;
  serveCount: string;
}

export interface MsgIncrementServeResponse {
}

export interface MsgRecordBan {
  authority: string;
  contributorId: string;
  orgId: string;
  memoryCid: string;
}

export interface MsgRecordBanResponse {
}

export interface MsgUpdateParams {
  authority: string;
  params?: Params | undefined;
}

export interface MsgUpdateParamsResponse {
}

function createBaseMsgUpdateReputation(): MsgUpdateReputation {
  return {
    signer: "",
    developer: new Uint8Array(0),
    memoryCid: "",
    difficulty: 0,
    quality: 0,
    domainTags: [],
    provenance: "",
  };
}

export const MsgUpdateReputation: MessageFns<MsgUpdateReputation> = {
  encode(message: MsgUpdateReputation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signer !== "") {
      writer.uint32(10).string(message.signer);
    }
    if (message.developer.length !== 0) {
      writer.uint32(18).bytes(message.developer);
    }
    if (message.memoryCid !== "") {
      writer.uint32(26).string(message.memoryCid);
    }
    if (message.difficulty !== 0) {
      writer.uint32(32).uint32(message.difficulty);
    }
    if (message.quality !== 0) {
      writer.uint32(40).uint32(message.quality);
    }
    for (const v of message.domainTags) {
      writer.uint32(50).string(v!);
    }
    if (message.provenance !== "") {
      writer.uint32(58).string(message.provenance);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateReputation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateReputation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signer = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.developer = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.memoryCid = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.difficulty = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.quality = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.domainTags.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.provenance = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateReputation {
    return {
      signer: isSet(object.signer) ? globalThis.String(object.signer) : "",
      developer: isSet(object.developer) ? bytesFromBase64(object.developer) : new Uint8Array(0),
      memoryCid: isSet(object.memoryCid)
        ? globalThis.String(object.memoryCid)
        : isSet(object.memory_cid)
        ? globalThis.String(object.memory_cid)
        : "",
      difficulty: isSet(object.difficulty) ? globalThis.Number(object.difficulty) : 0,
      quality: isSet(object.quality) ? globalThis.Number(object.quality) : 0,
      domainTags: globalThis.Array.isArray(object?.domainTags)
        ? object.domainTags.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.domain_tags)
        ? object.domain_tags.map((e: any) => globalThis.String(e))
        : [],
      provenance: isSet(object.provenance) ? globalThis.String(object.provenance) : "",
    };
  },

  toJSON(message: MsgUpdateReputation): unknown {
    const obj: any = {};
    if (message.signer !== "") {
      obj.signer = message.signer;
    }
    if (message.developer.length !== 0) {
      obj.developer = base64FromBytes(message.developer);
    }
    if (message.memoryCid !== "") {
      obj.memoryCid = message.memoryCid;
    }
    if (message.difficulty !== 0) {
      obj.difficulty = Math.round(message.difficulty);
    }
    if (message.quality !== 0) {
      obj.quality = Math.round(message.quality);
    }
    if (message.domainTags?.length) {
      obj.domainTags = message.domainTags;
    }
    if (message.provenance !== "") {
      obj.provenance = message.provenance;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateReputation>, I>>(base?: I): MsgUpdateReputation {
    return MsgUpdateReputation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateReputation>, I>>(object: I): MsgUpdateReputation {
    const message = createBaseMsgUpdateReputation();
    message.signer = object.signer ?? "";
    message.developer = object.developer ?? new Uint8Array(0);
    message.memoryCid = object.memoryCid ?? "";
    message.difficulty = object.difficulty ?? 0;
    message.quality = object.quality ?? 0;
    message.domainTags = object.domainTags?.map((e) => e) || [];
    message.provenance = object.provenance ?? "";
    return message;
  },
};

function createBaseMsgUpdateReputationResponse(): MsgUpdateReputationResponse {
  return { xp: "0" };
}

export const MsgUpdateReputationResponse: MessageFns<MsgUpdateReputationResponse> = {
  encode(message: MsgUpdateReputationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.xp !== "0") {
      writer.uint32(8).uint64(message.xp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateReputationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateReputationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.xp = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateReputationResponse {
    return { xp: isSet(object.xp) ? globalThis.String(object.xp) : "0" };
  },

  toJSON(message: MsgUpdateReputationResponse): unknown {
    const obj: any = {};
    if (message.xp !== "0") {
      obj.xp = message.xp;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateReputationResponse>, I>>(base?: I): MsgUpdateReputationResponse {
    return MsgUpdateReputationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateReputationResponse>, I>>(object: I): MsgUpdateReputationResponse {
    const message = createBaseMsgUpdateReputationResponse();
    message.xp = object.xp ?? "0";
    return message;
  },
};

function createBaseMsgIncrementContribution(): MsgIncrementContribution {
  return { authority: "", contributorId: "", orgId: "", memoryCid: "" };
}

export const MsgIncrementContribution: MessageFns<MsgIncrementContribution> = {
  encode(message: MsgIncrementContribution, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.contributorId !== "") {
      writer.uint32(18).string(message.contributorId);
    }
    if (message.orgId !== "") {
      writer.uint32(26).string(message.orgId);
    }
    if (message.memoryCid !== "") {
      writer.uint32(34).string(message.memoryCid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgIncrementContribution {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgIncrementContribution();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contributorId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.memoryCid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgIncrementContribution {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      contributorId: isSet(object.contributorId)
        ? globalThis.String(object.contributorId)
        : isSet(object.contributor_id)
        ? globalThis.String(object.contributor_id)
        : "",
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      memoryCid: isSet(object.memoryCid)
        ? globalThis.String(object.memoryCid)
        : isSet(object.memory_cid)
        ? globalThis.String(object.memory_cid)
        : "",
    };
  },

  toJSON(message: MsgIncrementContribution): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.contributorId !== "") {
      obj.contributorId = message.contributorId;
    }
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.memoryCid !== "") {
      obj.memoryCid = message.memoryCid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgIncrementContribution>, I>>(base?: I): MsgIncrementContribution {
    return MsgIncrementContribution.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgIncrementContribution>, I>>(object: I): MsgIncrementContribution {
    const message = createBaseMsgIncrementContribution();
    message.authority = object.authority ?? "";
    message.contributorId = object.contributorId ?? "";
    message.orgId = object.orgId ?? "";
    message.memoryCid = object.memoryCid ?? "";
    return message;
  },
};

function createBaseMsgIncrementContributionResponse(): MsgIncrementContributionResponse {
  return {};
}

export const MsgIncrementContributionResponse: MessageFns<MsgIncrementContributionResponse> = {
  encode(_: MsgIncrementContributionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgIncrementContributionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgIncrementContributionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgIncrementContributionResponse {
    return {};
  },

  toJSON(_: MsgIncrementContributionResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgIncrementContributionResponse>, I>>(
    base?: I,
  ): MsgIncrementContributionResponse {
    return MsgIncrementContributionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgIncrementContributionResponse>, I>>(
    _: I,
  ): MsgIncrementContributionResponse {
    const message = createBaseMsgIncrementContributionResponse();
    return message;
  },
};

function createBaseMsgIncrementServe(): MsgIncrementServe {
  return { authority: "", contributorId: "", orgId: "", memoryCid: "", serveCount: "0" };
}

export const MsgIncrementServe: MessageFns<MsgIncrementServe> = {
  encode(message: MsgIncrementServe, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.contributorId !== "") {
      writer.uint32(18).string(message.contributorId);
    }
    if (message.orgId !== "") {
      writer.uint32(26).string(message.orgId);
    }
    if (message.memoryCid !== "") {
      writer.uint32(34).string(message.memoryCid);
    }
    if (message.serveCount !== "0") {
      writer.uint32(40).uint64(message.serveCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgIncrementServe {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgIncrementServe();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contributorId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.memoryCid = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.serveCount = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgIncrementServe {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      contributorId: isSet(object.contributorId)
        ? globalThis.String(object.contributorId)
        : isSet(object.contributor_id)
        ? globalThis.String(object.contributor_id)
        : "",
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      memoryCid: isSet(object.memoryCid)
        ? globalThis.String(object.memoryCid)
        : isSet(object.memory_cid)
        ? globalThis.String(object.memory_cid)
        : "",
      serveCount: isSet(object.serveCount)
        ? globalThis.String(object.serveCount)
        : isSet(object.serve_count)
        ? globalThis.String(object.serve_count)
        : "0",
    };
  },

  toJSON(message: MsgIncrementServe): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.contributorId !== "") {
      obj.contributorId = message.contributorId;
    }
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.memoryCid !== "") {
      obj.memoryCid = message.memoryCid;
    }
    if (message.serveCount !== "0") {
      obj.serveCount = message.serveCount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgIncrementServe>, I>>(base?: I): MsgIncrementServe {
    return MsgIncrementServe.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgIncrementServe>, I>>(object: I): MsgIncrementServe {
    const message = createBaseMsgIncrementServe();
    message.authority = object.authority ?? "";
    message.contributorId = object.contributorId ?? "";
    message.orgId = object.orgId ?? "";
    message.memoryCid = object.memoryCid ?? "";
    message.serveCount = object.serveCount ?? "0";
    return message;
  },
};

function createBaseMsgIncrementServeResponse(): MsgIncrementServeResponse {
  return {};
}

export const MsgIncrementServeResponse: MessageFns<MsgIncrementServeResponse> = {
  encode(_: MsgIncrementServeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgIncrementServeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgIncrementServeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgIncrementServeResponse {
    return {};
  },

  toJSON(_: MsgIncrementServeResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgIncrementServeResponse>, I>>(base?: I): MsgIncrementServeResponse {
    return MsgIncrementServeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgIncrementServeResponse>, I>>(_: I): MsgIncrementServeResponse {
    const message = createBaseMsgIncrementServeResponse();
    return message;
  },
};

function createBaseMsgRecordBan(): MsgRecordBan {
  return { authority: "", contributorId: "", orgId: "", memoryCid: "" };
}

export const MsgRecordBan: MessageFns<MsgRecordBan> = {
  encode(message: MsgRecordBan, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.contributorId !== "") {
      writer.uint32(18).string(message.contributorId);
    }
    if (message.orgId !== "") {
      writer.uint32(26).string(message.orgId);
    }
    if (message.memoryCid !== "") {
      writer.uint32(34).string(message.memoryCid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRecordBan {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRecordBan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contributorId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.memoryCid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRecordBan {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      contributorId: isSet(object.contributorId)
        ? globalThis.String(object.contributorId)
        : isSet(object.contributor_id)
        ? globalThis.String(object.contributor_id)
        : "",
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      memoryCid: isSet(object.memoryCid)
        ? globalThis.String(object.memoryCid)
        : isSet(object.memory_cid)
        ? globalThis.String(object.memory_cid)
        : "",
    };
  },

  toJSON(message: MsgRecordBan): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.contributorId !== "") {
      obj.contributorId = message.contributorId;
    }
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.memoryCid !== "") {
      obj.memoryCid = message.memoryCid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRecordBan>, I>>(base?: I): MsgRecordBan {
    return MsgRecordBan.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRecordBan>, I>>(object: I): MsgRecordBan {
    const message = createBaseMsgRecordBan();
    message.authority = object.authority ?? "";
    message.contributorId = object.contributorId ?? "";
    message.orgId = object.orgId ?? "";
    message.memoryCid = object.memoryCid ?? "";
    return message;
  },
};

function createBaseMsgRecordBanResponse(): MsgRecordBanResponse {
  return {};
}

export const MsgRecordBanResponse: MessageFns<MsgRecordBanResponse> = {
  encode(_: MsgRecordBanResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRecordBanResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRecordBanResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRecordBanResponse {
    return {};
  },

  toJSON(_: MsgRecordBanResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRecordBanResponse>, I>>(base?: I): MsgRecordBanResponse {
    return MsgRecordBanResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRecordBanResponse>, I>>(_: I): MsgRecordBanResponse {
    const message = createBaseMsgRecordBanResponse();
    return message;
  },
};

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(base?: I): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(object: I): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(base?: I): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(_: I): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
