// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: wevibe/reputation/v1/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "wevibe.reputation.v1";

export interface Params {
  active: boolean;
  maxDifficulty: number;
  maxQuality: number;
  serveXpPerServe: string;
  selfServeXpPerServe: string;
}

function createBaseParams(): Params {
  return { active: false, maxDifficulty: 0, maxQuality: 0, serveXpPerServe: "0", selfServeXpPerServe: "0" };
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.active !== false) {
      writer.uint32(8).bool(message.active);
    }
    if (message.maxDifficulty !== 0) {
      writer.uint32(16).uint32(message.maxDifficulty);
    }
    if (message.maxQuality !== 0) {
      writer.uint32(24).uint32(message.maxQuality);
    }
    if (message.serveXpPerServe !== "0") {
      writer.uint32(32).uint64(message.serveXpPerServe);
    }
    if (message.selfServeXpPerServe !== "0") {
      writer.uint32(40).uint64(message.selfServeXpPerServe);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.active = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxDifficulty = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxQuality = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.serveXpPerServe = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.selfServeXpPerServe = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
      maxDifficulty: isSet(object.maxDifficulty)
        ? globalThis.Number(object.maxDifficulty)
        : isSet(object.max_difficulty)
        ? globalThis.Number(object.max_difficulty)
        : 0,
      maxQuality: isSet(object.maxQuality)
        ? globalThis.Number(object.maxQuality)
        : isSet(object.max_quality)
        ? globalThis.Number(object.max_quality)
        : 0,
      serveXpPerServe: isSet(object.serveXpPerServe)
        ? globalThis.String(object.serveXpPerServe)
        : isSet(object.serve_xp_per_serve)
        ? globalThis.String(object.serve_xp_per_serve)
        : "0",
      selfServeXpPerServe: isSet(object.selfServeXpPerServe)
        ? globalThis.String(object.selfServeXpPerServe)
        : isSet(object.self_serve_xp_per_serve)
        ? globalThis.String(object.self_serve_xp_per_serve)
        : "0",
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.active !== false) {
      obj.active = message.active;
    }
    if (message.maxDifficulty !== 0) {
      obj.maxDifficulty = Math.round(message.maxDifficulty);
    }
    if (message.maxQuality !== 0) {
      obj.maxQuality = Math.round(message.maxQuality);
    }
    if (message.serveXpPerServe !== "0") {
      obj.serveXpPerServe = message.serveXpPerServe;
    }
    if (message.selfServeXpPerServe !== "0") {
      obj.selfServeXpPerServe = message.selfServeXpPerServe;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams();
    message.active = object.active ?? false;
    message.maxDifficulty = object.maxDifficulty ?? 0;
    message.maxQuality = object.maxQuality ?? 0;
    message.serveXpPerServe = object.serveXpPerServe ?? "0";
    message.selfServeXpPerServe = object.selfServeXpPerServe ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
