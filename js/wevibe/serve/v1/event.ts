// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: wevibe/serve/v1/event.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "wevibe.serve.v1";

/**
 * RECALL-PIVOT event log (RECALL-PIVOT-SPEC §3). Events are immutable,
 * append-only, content-free, consumer-signed observations with provenance.
 * BOUNDARY RULE (§3.2): no verdicts, weights, standing, scores, trust values,
 * archive flags, derived judgments, or content may ever appear in an event.
 */
export enum EventType {
  EVENT_TYPE_UNSPECIFIED = 0,
  /** EVENT_TYPE_SERVE - E1 — recorded via serve receipts / StoredServeReceipt. */
  EVENT_TYPE_SERVE = 1,
  /** EVENT_TYPE_BLOCK - E2 — recorded via denial receipts / StoredDenialReceipt. */
  EVENT_TYPE_BLOCK = 2,
  /** EVENT_TYPE_OUTCOME - E3 use-leg, load-bearing. */
  EVENT_TYPE_OUTCOME = 3,
  /** EVENT_TYPE_CONTEST - E4 PARKED — schema slot only, activation deferred pending supersession. */
  EVENT_TYPE_CONTEST = 4,
  /** EVENT_TYPE_SPONSORSHIP - E5 PARKED — pending ROADMAP economics decision. */
  EVENT_TYPE_SPONSORSHIP = 5,
  EVENT_TYPE_VALIDITY_PREDICATE = 6,
  EVENT_TYPE_COST_TO_DISCOVER = 7,
  EVENT_TYPE_CONVERGENCE = 8,
  UNRECOGNIZED = -1,
}

export function eventTypeFromJSON(object: any): EventType {
  switch (object) {
    case 0:
    case "EVENT_TYPE_UNSPECIFIED":
      return EventType.EVENT_TYPE_UNSPECIFIED;
    case 1:
    case "EVENT_TYPE_SERVE":
      return EventType.EVENT_TYPE_SERVE;
    case 2:
    case "EVENT_TYPE_BLOCK":
      return EventType.EVENT_TYPE_BLOCK;
    case 3:
    case "EVENT_TYPE_OUTCOME":
      return EventType.EVENT_TYPE_OUTCOME;
    case 4:
    case "EVENT_TYPE_CONTEST":
      return EventType.EVENT_TYPE_CONTEST;
    case 5:
    case "EVENT_TYPE_SPONSORSHIP":
      return EventType.EVENT_TYPE_SPONSORSHIP;
    case 6:
    case "EVENT_TYPE_VALIDITY_PREDICATE":
      return EventType.EVENT_TYPE_VALIDITY_PREDICATE;
    case 7:
    case "EVENT_TYPE_COST_TO_DISCOVER":
      return EventType.EVENT_TYPE_COST_TO_DISCOVER;
    case 8:
    case "EVENT_TYPE_CONVERGENCE":
      return EventType.EVENT_TYPE_CONVERGENCE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EventType.UNRECOGNIZED;
  }
}

export function eventTypeToJSON(object: EventType): string {
  switch (object) {
    case EventType.EVENT_TYPE_UNSPECIFIED:
      return "EVENT_TYPE_UNSPECIFIED";
    case EventType.EVENT_TYPE_SERVE:
      return "EVENT_TYPE_SERVE";
    case EventType.EVENT_TYPE_BLOCK:
      return "EVENT_TYPE_BLOCK";
    case EventType.EVENT_TYPE_OUTCOME:
      return "EVENT_TYPE_OUTCOME";
    case EventType.EVENT_TYPE_CONTEST:
      return "EVENT_TYPE_CONTEST";
    case EventType.EVENT_TYPE_SPONSORSHIP:
      return "EVENT_TYPE_SPONSORSHIP";
    case EventType.EVENT_TYPE_VALIDITY_PREDICATE:
      return "EVENT_TYPE_VALIDITY_PREDICATE";
    case EventType.EVENT_TYPE_COST_TO_DISCOVER:
      return "EVENT_TYPE_COST_TO_DISCOVER";
    case EventType.EVENT_TYPE_CONVERGENCE:
      return "EVENT_TYPE_CONVERGENCE";
    case EventType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum PredicateResult {
  PREDICATE_RESULT_UNSPECIFIED = 0,
  PREDICATE_RESULT_PASS = 1,
  PREDICATE_RESULT_FAIL = 2,
  /** PREDICATE_RESULT_ABSENT - Fail-closed — a timed-out/failed predicate records ABSENCE, never an inferred result. */
  PREDICATE_RESULT_ABSENT = 3,
  UNRECOGNIZED = -1,
}

export function predicateResultFromJSON(object: any): PredicateResult {
  switch (object) {
    case 0:
    case "PREDICATE_RESULT_UNSPECIFIED":
      return PredicateResult.PREDICATE_RESULT_UNSPECIFIED;
    case 1:
    case "PREDICATE_RESULT_PASS":
      return PredicateResult.PREDICATE_RESULT_PASS;
    case 2:
    case "PREDICATE_RESULT_FAIL":
      return PredicateResult.PREDICATE_RESULT_FAIL;
    case 3:
    case "PREDICATE_RESULT_ABSENT":
      return PredicateResult.PREDICATE_RESULT_ABSENT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PredicateResult.UNRECOGNIZED;
  }
}

export function predicateResultToJSON(object: PredicateResult): string {
  switch (object) {
    case PredicateResult.PREDICATE_RESULT_UNSPECIFIED:
      return "PREDICATE_RESULT_UNSPECIFIED";
    case PredicateResult.PREDICATE_RESULT_PASS:
      return "PREDICATE_RESULT_PASS";
    case PredicateResult.PREDICATE_RESULT_FAIL:
      return "PREDICATE_RESULT_FAIL";
    case PredicateResult.PREDICATE_RESULT_ABSENT:
      return "PREDICATE_RESULT_ABSENT";
    case PredicateResult.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface OutcomeEventBody {
  /** Fingerprint of session identity + need reference — content-free. */
  episodeRef: Uint8Array;
  /** Raw observed fact bound to evidence — never a verdict. */
  worked: boolean;
  /** GSTV-sealed pattern reference. */
  evidenceRef: Uint8Array;
}

export interface ValidityPredicateEventBody {
  /** Hash. */
  predicateId: Uint8Array;
  result: PredicateResult;
  evidenceRef: Uint8Array;
}

export interface CostToDiscoverEventBody {
  cycles: string;
  toolCalls: string;
  attemptsToGreen: number;
  evidenceRef: Uint8Array;
}

export interface ConvergenceEventBody {
  /** Independent rediscovery evidence reference. */
  convergenceRef: Uint8Array;
}

export interface EventEntry {
  eventType: EventType;
  memoryContentHash: Uint8Array;
  /** Consumer per-org serve key, ed25519 — D-SERVE-CONSUMER-SIGNED lineage. */
  signerPubkey: Uint8Array;
  nonce: Uint8Array;
  /** Ed25519 over CanonicalEventBody. */
  signature: Uint8Array;
  outcome?: OutcomeEventBody | undefined;
  validityPredicate?: ValidityPredicateEventBody | undefined;
  costToDiscover?: CostToDiscoverEventBody | undefined;
  convergence?: ConvergenceEventBody | undefined;
}

export interface StoredEvent {
  orgId: string;
  epoch: string;
  eventType: EventType;
  memoryContentHash: Uint8Array;
  signerPubkey: Uint8Array;
  nonce: Uint8Array;
  signature: Uint8Array;
  /** Sha256(CanonicalEventBody) — dedup key. */
  fingerprint: Uint8Array;
  outcome?: OutcomeEventBody | undefined;
  validityPredicate?: ValidityPredicateEventBody | undefined;
  costToDiscover?: CostToDiscoverEventBody | undefined;
  convergence?: ConvergenceEventBody | undefined;
}

export interface StoredPolicyAnchor {
  /**
   * Published edge-policy version identifier — standing is NEVER written on-chain;
   * it is recomputed at the edge as f(events, policy_version).
   */
  policyVersion: string;
  /** Sha256 of the published policy artifact. */
  policyHash: Uint8Array;
  anchoredAtEpoch: string;
  anchoredAtHeight: string;
}

function createBaseOutcomeEventBody(): OutcomeEventBody {
  return { episodeRef: new Uint8Array(0), worked: false, evidenceRef: new Uint8Array(0) };
}

export const OutcomeEventBody: MessageFns<OutcomeEventBody> = {
  encode(message: OutcomeEventBody, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.episodeRef.length !== 0) {
      writer.uint32(10).bytes(message.episodeRef);
    }
    if (message.worked !== false) {
      writer.uint32(16).bool(message.worked);
    }
    if (message.evidenceRef.length !== 0) {
      writer.uint32(26).bytes(message.evidenceRef);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OutcomeEventBody {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOutcomeEventBody();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.episodeRef = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.worked = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.evidenceRef = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OutcomeEventBody {
    return {
      episodeRef: isSet(object.episodeRef)
        ? bytesFromBase64(object.episodeRef)
        : isSet(object.episode_ref)
        ? bytesFromBase64(object.episode_ref)
        : new Uint8Array(0),
      worked: isSet(object.worked) ? globalThis.Boolean(object.worked) : false,
      evidenceRef: isSet(object.evidenceRef)
        ? bytesFromBase64(object.evidenceRef)
        : isSet(object.evidence_ref)
        ? bytesFromBase64(object.evidence_ref)
        : new Uint8Array(0),
    };
  },

  toJSON(message: OutcomeEventBody): unknown {
    const obj: any = {};
    if (message.episodeRef.length !== 0) {
      obj.episodeRef = base64FromBytes(message.episodeRef);
    }
    if (message.worked !== false) {
      obj.worked = message.worked;
    }
    if (message.evidenceRef.length !== 0) {
      obj.evidenceRef = base64FromBytes(message.evidenceRef);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OutcomeEventBody>, I>>(base?: I): OutcomeEventBody {
    return OutcomeEventBody.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OutcomeEventBody>, I>>(object: I): OutcomeEventBody {
    const message = createBaseOutcomeEventBody();
    message.episodeRef = object.episodeRef ?? new Uint8Array(0);
    message.worked = object.worked ?? false;
    message.evidenceRef = object.evidenceRef ?? new Uint8Array(0);
    return message;
  },
};

function createBaseValidityPredicateEventBody(): ValidityPredicateEventBody {
  return { predicateId: new Uint8Array(0), result: 0, evidenceRef: new Uint8Array(0) };
}

export const ValidityPredicateEventBody: MessageFns<ValidityPredicateEventBody> = {
  encode(message: ValidityPredicateEventBody, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.predicateId.length !== 0) {
      writer.uint32(10).bytes(message.predicateId);
    }
    if (message.result !== 0) {
      writer.uint32(16).int32(message.result);
    }
    if (message.evidenceRef.length !== 0) {
      writer.uint32(26).bytes(message.evidenceRef);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidityPredicateEventBody {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidityPredicateEventBody();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.predicateId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.result = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.evidenceRef = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidityPredicateEventBody {
    return {
      predicateId: isSet(object.predicateId)
        ? bytesFromBase64(object.predicateId)
        : isSet(object.predicate_id)
        ? bytesFromBase64(object.predicate_id)
        : new Uint8Array(0),
      result: isSet(object.result) ? predicateResultFromJSON(object.result) : 0,
      evidenceRef: isSet(object.evidenceRef)
        ? bytesFromBase64(object.evidenceRef)
        : isSet(object.evidence_ref)
        ? bytesFromBase64(object.evidence_ref)
        : new Uint8Array(0),
    };
  },

  toJSON(message: ValidityPredicateEventBody): unknown {
    const obj: any = {};
    if (message.predicateId.length !== 0) {
      obj.predicateId = base64FromBytes(message.predicateId);
    }
    if (message.result !== 0) {
      obj.result = predicateResultToJSON(message.result);
    }
    if (message.evidenceRef.length !== 0) {
      obj.evidenceRef = base64FromBytes(message.evidenceRef);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidityPredicateEventBody>, I>>(base?: I): ValidityPredicateEventBody {
    return ValidityPredicateEventBody.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidityPredicateEventBody>, I>>(object: I): ValidityPredicateEventBody {
    const message = createBaseValidityPredicateEventBody();
    message.predicateId = object.predicateId ?? new Uint8Array(0);
    message.result = object.result ?? 0;
    message.evidenceRef = object.evidenceRef ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCostToDiscoverEventBody(): CostToDiscoverEventBody {
  return { cycles: "0", toolCalls: "0", attemptsToGreen: 0, evidenceRef: new Uint8Array(0) };
}

export const CostToDiscoverEventBody: MessageFns<CostToDiscoverEventBody> = {
  encode(message: CostToDiscoverEventBody, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cycles !== "0") {
      writer.uint32(8).uint64(message.cycles);
    }
    if (message.toolCalls !== "0") {
      writer.uint32(16).uint64(message.toolCalls);
    }
    if (message.attemptsToGreen !== 0) {
      writer.uint32(24).uint32(message.attemptsToGreen);
    }
    if (message.evidenceRef.length !== 0) {
      writer.uint32(34).bytes(message.evidenceRef);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CostToDiscoverEventBody {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCostToDiscoverEventBody();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cycles = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.toolCalls = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.attemptsToGreen = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.evidenceRef = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CostToDiscoverEventBody {
    return {
      cycles: isSet(object.cycles) ? globalThis.String(object.cycles) : "0",
      toolCalls: isSet(object.toolCalls)
        ? globalThis.String(object.toolCalls)
        : isSet(object.tool_calls)
        ? globalThis.String(object.tool_calls)
        : "0",
      attemptsToGreen: isSet(object.attemptsToGreen)
        ? globalThis.Number(object.attemptsToGreen)
        : isSet(object.attempts_to_green)
        ? globalThis.Number(object.attempts_to_green)
        : 0,
      evidenceRef: isSet(object.evidenceRef)
        ? bytesFromBase64(object.evidenceRef)
        : isSet(object.evidence_ref)
        ? bytesFromBase64(object.evidence_ref)
        : new Uint8Array(0),
    };
  },

  toJSON(message: CostToDiscoverEventBody): unknown {
    const obj: any = {};
    if (message.cycles !== "0") {
      obj.cycles = message.cycles;
    }
    if (message.toolCalls !== "0") {
      obj.toolCalls = message.toolCalls;
    }
    if (message.attemptsToGreen !== 0) {
      obj.attemptsToGreen = Math.round(message.attemptsToGreen);
    }
    if (message.evidenceRef.length !== 0) {
      obj.evidenceRef = base64FromBytes(message.evidenceRef);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CostToDiscoverEventBody>, I>>(base?: I): CostToDiscoverEventBody {
    return CostToDiscoverEventBody.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CostToDiscoverEventBody>, I>>(object: I): CostToDiscoverEventBody {
    const message = createBaseCostToDiscoverEventBody();
    message.cycles = object.cycles ?? "0";
    message.toolCalls = object.toolCalls ?? "0";
    message.attemptsToGreen = object.attemptsToGreen ?? 0;
    message.evidenceRef = object.evidenceRef ?? new Uint8Array(0);
    return message;
  },
};

function createBaseConvergenceEventBody(): ConvergenceEventBody {
  return { convergenceRef: new Uint8Array(0) };
}

export const ConvergenceEventBody: MessageFns<ConvergenceEventBody> = {
  encode(message: ConvergenceEventBody, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.convergenceRef.length !== 0) {
      writer.uint32(10).bytes(message.convergenceRef);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConvergenceEventBody {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConvergenceEventBody();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.convergenceRef = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConvergenceEventBody {
    return {
      convergenceRef: isSet(object.convergenceRef)
        ? bytesFromBase64(object.convergenceRef)
        : isSet(object.convergence_ref)
        ? bytesFromBase64(object.convergence_ref)
        : new Uint8Array(0),
    };
  },

  toJSON(message: ConvergenceEventBody): unknown {
    const obj: any = {};
    if (message.convergenceRef.length !== 0) {
      obj.convergenceRef = base64FromBytes(message.convergenceRef);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConvergenceEventBody>, I>>(base?: I): ConvergenceEventBody {
    return ConvergenceEventBody.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConvergenceEventBody>, I>>(object: I): ConvergenceEventBody {
    const message = createBaseConvergenceEventBody();
    message.convergenceRef = object.convergenceRef ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEventEntry(): EventEntry {
  return {
    eventType: 0,
    memoryContentHash: new Uint8Array(0),
    signerPubkey: new Uint8Array(0),
    nonce: new Uint8Array(0),
    signature: new Uint8Array(0),
    outcome: undefined,
    validityPredicate: undefined,
    costToDiscover: undefined,
    convergence: undefined,
  };
}

export const EventEntry: MessageFns<EventEntry> = {
  encode(message: EventEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventType !== 0) {
      writer.uint32(8).int32(message.eventType);
    }
    if (message.memoryContentHash.length !== 0) {
      writer.uint32(18).bytes(message.memoryContentHash);
    }
    if (message.signerPubkey.length !== 0) {
      writer.uint32(26).bytes(message.signerPubkey);
    }
    if (message.nonce.length !== 0) {
      writer.uint32(34).bytes(message.nonce);
    }
    if (message.signature.length !== 0) {
      writer.uint32(42).bytes(message.signature);
    }
    if (message.outcome !== undefined) {
      OutcomeEventBody.encode(message.outcome, writer.uint32(82).fork()).join();
    }
    if (message.validityPredicate !== undefined) {
      ValidityPredicateEventBody.encode(message.validityPredicate, writer.uint32(90).fork()).join();
    }
    if (message.costToDiscover !== undefined) {
      CostToDiscoverEventBody.encode(message.costToDiscover, writer.uint32(98).fork()).join();
    }
    if (message.convergence !== undefined) {
      ConvergenceEventBody.encode(message.convergence, writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.eventType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.memoryContentHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.signerPubkey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.nonce = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.outcome = OutcomeEventBody.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.validityPredicate = ValidityPredicateEventBody.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.costToDiscover = CostToDiscoverEventBody.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.convergence = ConvergenceEventBody.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventEntry {
    return {
      eventType: isSet(object.eventType)
        ? eventTypeFromJSON(object.eventType)
        : isSet(object.event_type)
        ? eventTypeFromJSON(object.event_type)
        : 0,
      memoryContentHash: isSet(object.memoryContentHash)
        ? bytesFromBase64(object.memoryContentHash)
        : isSet(object.memory_content_hash)
        ? bytesFromBase64(object.memory_content_hash)
        : new Uint8Array(0),
      signerPubkey: isSet(object.signerPubkey)
        ? bytesFromBase64(object.signerPubkey)
        : isSet(object.signer_pubkey)
        ? bytesFromBase64(object.signer_pubkey)
        : new Uint8Array(0),
      nonce: isSet(object.nonce) ? bytesFromBase64(object.nonce) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      outcome: isSet(object.outcome) ? OutcomeEventBody.fromJSON(object.outcome) : undefined,
      validityPredicate: isSet(object.validityPredicate)
        ? ValidityPredicateEventBody.fromJSON(object.validityPredicate)
        : isSet(object.validity_predicate)
        ? ValidityPredicateEventBody.fromJSON(object.validity_predicate)
        : undefined,
      costToDiscover: isSet(object.costToDiscover)
        ? CostToDiscoverEventBody.fromJSON(object.costToDiscover)
        : isSet(object.cost_to_discover)
        ? CostToDiscoverEventBody.fromJSON(object.cost_to_discover)
        : undefined,
      convergence: isSet(object.convergence) ? ConvergenceEventBody.fromJSON(object.convergence) : undefined,
    };
  },

  toJSON(message: EventEntry): unknown {
    const obj: any = {};
    if (message.eventType !== 0) {
      obj.eventType = eventTypeToJSON(message.eventType);
    }
    if (message.memoryContentHash.length !== 0) {
      obj.memoryContentHash = base64FromBytes(message.memoryContentHash);
    }
    if (message.signerPubkey.length !== 0) {
      obj.signerPubkey = base64FromBytes(message.signerPubkey);
    }
    if (message.nonce.length !== 0) {
      obj.nonce = base64FromBytes(message.nonce);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.outcome !== undefined) {
      obj.outcome = OutcomeEventBody.toJSON(message.outcome);
    }
    if (message.validityPredicate !== undefined) {
      obj.validityPredicate = ValidityPredicateEventBody.toJSON(message.validityPredicate);
    }
    if (message.costToDiscover !== undefined) {
      obj.costToDiscover = CostToDiscoverEventBody.toJSON(message.costToDiscover);
    }
    if (message.convergence !== undefined) {
      obj.convergence = ConvergenceEventBody.toJSON(message.convergence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventEntry>, I>>(base?: I): EventEntry {
    return EventEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventEntry>, I>>(object: I): EventEntry {
    const message = createBaseEventEntry();
    message.eventType = object.eventType ?? 0;
    message.memoryContentHash = object.memoryContentHash ?? new Uint8Array(0);
    message.signerPubkey = object.signerPubkey ?? new Uint8Array(0);
    message.nonce = object.nonce ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    message.outcome = (object.outcome !== undefined && object.outcome !== null)
      ? OutcomeEventBody.fromPartial(object.outcome)
      : undefined;
    message.validityPredicate = (object.validityPredicate !== undefined && object.validityPredicate !== null)
      ? ValidityPredicateEventBody.fromPartial(object.validityPredicate)
      : undefined;
    message.costToDiscover = (object.costToDiscover !== undefined && object.costToDiscover !== null)
      ? CostToDiscoverEventBody.fromPartial(object.costToDiscover)
      : undefined;
    message.convergence = (object.convergence !== undefined && object.convergence !== null)
      ? ConvergenceEventBody.fromPartial(object.convergence)
      : undefined;
    return message;
  },
};

function createBaseStoredEvent(): StoredEvent {
  return {
    orgId: "",
    epoch: "0",
    eventType: 0,
    memoryContentHash: new Uint8Array(0),
    signerPubkey: new Uint8Array(0),
    nonce: new Uint8Array(0),
    signature: new Uint8Array(0),
    fingerprint: new Uint8Array(0),
    outcome: undefined,
    validityPredicate: undefined,
    costToDiscover: undefined,
    convergence: undefined,
  };
}

export const StoredEvent: MessageFns<StoredEvent> = {
  encode(message: StoredEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    if (message.epoch !== "0") {
      writer.uint32(16).uint64(message.epoch);
    }
    if (message.eventType !== 0) {
      writer.uint32(24).int32(message.eventType);
    }
    if (message.memoryContentHash.length !== 0) {
      writer.uint32(34).bytes(message.memoryContentHash);
    }
    if (message.signerPubkey.length !== 0) {
      writer.uint32(42).bytes(message.signerPubkey);
    }
    if (message.nonce.length !== 0) {
      writer.uint32(50).bytes(message.nonce);
    }
    if (message.signature.length !== 0) {
      writer.uint32(58).bytes(message.signature);
    }
    if (message.fingerprint.length !== 0) {
      writer.uint32(66).bytes(message.fingerprint);
    }
    if (message.outcome !== undefined) {
      OutcomeEventBody.encode(message.outcome, writer.uint32(82).fork()).join();
    }
    if (message.validityPredicate !== undefined) {
      ValidityPredicateEventBody.encode(message.validityPredicate, writer.uint32(90).fork()).join();
    }
    if (message.costToDiscover !== undefined) {
      CostToDiscoverEventBody.encode(message.costToDiscover, writer.uint32(98).fork()).join();
    }
    if (message.convergence !== undefined) {
      ConvergenceEventBody.encode(message.convergence, writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.epoch = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.eventType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.memoryContentHash = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signerPubkey = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.nonce = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.fingerprint = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.outcome = OutcomeEventBody.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.validityPredicate = ValidityPredicateEventBody.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.costToDiscover = CostToDiscoverEventBody.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.convergence = ConvergenceEventBody.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredEvent {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
      eventType: isSet(object.eventType)
        ? eventTypeFromJSON(object.eventType)
        : isSet(object.event_type)
        ? eventTypeFromJSON(object.event_type)
        : 0,
      memoryContentHash: isSet(object.memoryContentHash)
        ? bytesFromBase64(object.memoryContentHash)
        : isSet(object.memory_content_hash)
        ? bytesFromBase64(object.memory_content_hash)
        : new Uint8Array(0),
      signerPubkey: isSet(object.signerPubkey)
        ? bytesFromBase64(object.signerPubkey)
        : isSet(object.signer_pubkey)
        ? bytesFromBase64(object.signer_pubkey)
        : new Uint8Array(0),
      nonce: isSet(object.nonce) ? bytesFromBase64(object.nonce) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      fingerprint: isSet(object.fingerprint) ? bytesFromBase64(object.fingerprint) : new Uint8Array(0),
      outcome: isSet(object.outcome) ? OutcomeEventBody.fromJSON(object.outcome) : undefined,
      validityPredicate: isSet(object.validityPredicate)
        ? ValidityPredicateEventBody.fromJSON(object.validityPredicate)
        : isSet(object.validity_predicate)
        ? ValidityPredicateEventBody.fromJSON(object.validity_predicate)
        : undefined,
      costToDiscover: isSet(object.costToDiscover)
        ? CostToDiscoverEventBody.fromJSON(object.costToDiscover)
        : isSet(object.cost_to_discover)
        ? CostToDiscoverEventBody.fromJSON(object.cost_to_discover)
        : undefined,
      convergence: isSet(object.convergence) ? ConvergenceEventBody.fromJSON(object.convergence) : undefined,
    };
  },

  toJSON(message: StoredEvent): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    if (message.eventType !== 0) {
      obj.eventType = eventTypeToJSON(message.eventType);
    }
    if (message.memoryContentHash.length !== 0) {
      obj.memoryContentHash = base64FromBytes(message.memoryContentHash);
    }
    if (message.signerPubkey.length !== 0) {
      obj.signerPubkey = base64FromBytes(message.signerPubkey);
    }
    if (message.nonce.length !== 0) {
      obj.nonce = base64FromBytes(message.nonce);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.fingerprint.length !== 0) {
      obj.fingerprint = base64FromBytes(message.fingerprint);
    }
    if (message.outcome !== undefined) {
      obj.outcome = OutcomeEventBody.toJSON(message.outcome);
    }
    if (message.validityPredicate !== undefined) {
      obj.validityPredicate = ValidityPredicateEventBody.toJSON(message.validityPredicate);
    }
    if (message.costToDiscover !== undefined) {
      obj.costToDiscover = CostToDiscoverEventBody.toJSON(message.costToDiscover);
    }
    if (message.convergence !== undefined) {
      obj.convergence = ConvergenceEventBody.toJSON(message.convergence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredEvent>, I>>(base?: I): StoredEvent {
    return StoredEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredEvent>, I>>(object: I): StoredEvent {
    const message = createBaseStoredEvent();
    message.orgId = object.orgId ?? "";
    message.epoch = object.epoch ?? "0";
    message.eventType = object.eventType ?? 0;
    message.memoryContentHash = object.memoryContentHash ?? new Uint8Array(0);
    message.signerPubkey = object.signerPubkey ?? new Uint8Array(0);
    message.nonce = object.nonce ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    message.fingerprint = object.fingerprint ?? new Uint8Array(0);
    message.outcome = (object.outcome !== undefined && object.outcome !== null)
      ? OutcomeEventBody.fromPartial(object.outcome)
      : undefined;
    message.validityPredicate = (object.validityPredicate !== undefined && object.validityPredicate !== null)
      ? ValidityPredicateEventBody.fromPartial(object.validityPredicate)
      : undefined;
    message.costToDiscover = (object.costToDiscover !== undefined && object.costToDiscover !== null)
      ? CostToDiscoverEventBody.fromPartial(object.costToDiscover)
      : undefined;
    message.convergence = (object.convergence !== undefined && object.convergence !== null)
      ? ConvergenceEventBody.fromPartial(object.convergence)
      : undefined;
    return message;
  },
};

function createBaseStoredPolicyAnchor(): StoredPolicyAnchor {
  return { policyVersion: "", policyHash: new Uint8Array(0), anchoredAtEpoch: "0", anchoredAtHeight: "0" };
}

export const StoredPolicyAnchor: MessageFns<StoredPolicyAnchor> = {
  encode(message: StoredPolicyAnchor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.policyVersion !== "") {
      writer.uint32(10).string(message.policyVersion);
    }
    if (message.policyHash.length !== 0) {
      writer.uint32(18).bytes(message.policyHash);
    }
    if (message.anchoredAtEpoch !== "0") {
      writer.uint32(24).uint64(message.anchoredAtEpoch);
    }
    if (message.anchoredAtHeight !== "0") {
      writer.uint32(32).int64(message.anchoredAtHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredPolicyAnchor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredPolicyAnchor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.policyVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policyHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.anchoredAtEpoch = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.anchoredAtHeight = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredPolicyAnchor {
    return {
      policyVersion: isSet(object.policyVersion)
        ? globalThis.String(object.policyVersion)
        : isSet(object.policy_version)
        ? globalThis.String(object.policy_version)
        : "",
      policyHash: isSet(object.policyHash)
        ? bytesFromBase64(object.policyHash)
        : isSet(object.policy_hash)
        ? bytesFromBase64(object.policy_hash)
        : new Uint8Array(0),
      anchoredAtEpoch: isSet(object.anchoredAtEpoch)
        ? globalThis.String(object.anchoredAtEpoch)
        : isSet(object.anchored_at_epoch)
        ? globalThis.String(object.anchored_at_epoch)
        : "0",
      anchoredAtHeight: isSet(object.anchoredAtHeight)
        ? globalThis.String(object.anchoredAtHeight)
        : isSet(object.anchored_at_height)
        ? globalThis.String(object.anchored_at_height)
        : "0",
    };
  },

  toJSON(message: StoredPolicyAnchor): unknown {
    const obj: any = {};
    if (message.policyVersion !== "") {
      obj.policyVersion = message.policyVersion;
    }
    if (message.policyHash.length !== 0) {
      obj.policyHash = base64FromBytes(message.policyHash);
    }
    if (message.anchoredAtEpoch !== "0") {
      obj.anchoredAtEpoch = message.anchoredAtEpoch;
    }
    if (message.anchoredAtHeight !== "0") {
      obj.anchoredAtHeight = message.anchoredAtHeight;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredPolicyAnchor>, I>>(base?: I): StoredPolicyAnchor {
    return StoredPolicyAnchor.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredPolicyAnchor>, I>>(object: I): StoredPolicyAnchor {
    const message = createBaseStoredPolicyAnchor();
    message.policyVersion = object.policyVersion ?? "";
    message.policyHash = object.policyHash ?? new Uint8Array(0);
    message.anchoredAtEpoch = object.anchoredAtEpoch ?? "0";
    message.anchoredAtHeight = object.anchoredAtHeight ?? "0";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
