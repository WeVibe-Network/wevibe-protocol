// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: wevibe/attestation/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";
import { ProviderType, providerTypeFromJSON, providerTypeToJSON } from "./state";

export const protobufPackage = "wevibe.attestation.v1";

export interface MsgSubmitSessionAttestation {
  signer: string;
  orgId: string;
  sessionHash: Uint8Array;
  modelId: string;
  turnCount: number;
  tokenCount: number;
  providerType: ProviderType;
  commitllmReceiptHash: Uint8Array;
  providerSignatureHash: Uint8Array;
  contributorId: string;
  epoch: string;
}

export interface MsgSubmitSessionAttestationResponse {
  accepted: boolean;
  verificationStatus: string;
}

export interface MsgUpdateParams {
  authority: string;
  params?: Params | undefined;
}

export interface MsgUpdateParamsResponse {
}

function createBaseMsgSubmitSessionAttestation(): MsgSubmitSessionAttestation {
  return {
    signer: "",
    orgId: "",
    sessionHash: new Uint8Array(0),
    modelId: "",
    turnCount: 0,
    tokenCount: 0,
    providerType: 0,
    commitllmReceiptHash: new Uint8Array(0),
    providerSignatureHash: new Uint8Array(0),
    contributorId: "",
    epoch: "0",
  };
}

export const MsgSubmitSessionAttestation: MessageFns<MsgSubmitSessionAttestation> = {
  encode(message: MsgSubmitSessionAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signer !== "") {
      writer.uint32(10).string(message.signer);
    }
    if (message.orgId !== "") {
      writer.uint32(18).string(message.orgId);
    }
    if (message.sessionHash.length !== 0) {
      writer.uint32(26).bytes(message.sessionHash);
    }
    if (message.modelId !== "") {
      writer.uint32(34).string(message.modelId);
    }
    if (message.turnCount !== 0) {
      writer.uint32(40).uint32(message.turnCount);
    }
    if (message.tokenCount !== 0) {
      writer.uint32(48).uint32(message.tokenCount);
    }
    if (message.providerType !== 0) {
      writer.uint32(56).int32(message.providerType);
    }
    if (message.commitllmReceiptHash.length !== 0) {
      writer.uint32(66).bytes(message.commitllmReceiptHash);
    }
    if (message.providerSignatureHash.length !== 0) {
      writer.uint32(74).bytes(message.providerSignatureHash);
    }
    if (message.contributorId !== "") {
      writer.uint32(82).string(message.contributorId);
    }
    if (message.epoch !== "0") {
      writer.uint32(88).uint64(message.epoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgSubmitSessionAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSubmitSessionAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signer = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessionHash = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.turnCount = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.tokenCount = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.providerType = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.commitllmReceiptHash = reader.bytes();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.providerSignatureHash = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.contributorId = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.epoch = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSubmitSessionAttestation {
    return {
      signer: isSet(object.signer) ? globalThis.String(object.signer) : "",
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      sessionHash: isSet(object.sessionHash)
        ? bytesFromBase64(object.sessionHash)
        : isSet(object.session_hash)
        ? bytesFromBase64(object.session_hash)
        : new Uint8Array(0),
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      turnCount: isSet(object.turnCount)
        ? globalThis.Number(object.turnCount)
        : isSet(object.turn_count)
        ? globalThis.Number(object.turn_count)
        : 0,
      tokenCount: isSet(object.tokenCount)
        ? globalThis.Number(object.tokenCount)
        : isSet(object.token_count)
        ? globalThis.Number(object.token_count)
        : 0,
      providerType: isSet(object.providerType)
        ? providerTypeFromJSON(object.providerType)
        : isSet(object.provider_type)
        ? providerTypeFromJSON(object.provider_type)
        : 0,
      commitllmReceiptHash: isSet(object.commitllmReceiptHash)
        ? bytesFromBase64(object.commitllmReceiptHash)
        : isSet(object.commitllm_receipt_hash)
        ? bytesFromBase64(object.commitllm_receipt_hash)
        : new Uint8Array(0),
      providerSignatureHash: isSet(object.providerSignatureHash)
        ? bytesFromBase64(object.providerSignatureHash)
        : isSet(object.provider_signature_hash)
        ? bytesFromBase64(object.provider_signature_hash)
        : new Uint8Array(0),
      contributorId: isSet(object.contributorId)
        ? globalThis.String(object.contributorId)
        : isSet(object.contributor_id)
        ? globalThis.String(object.contributor_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
    };
  },

  toJSON(message: MsgSubmitSessionAttestation): unknown {
    const obj: any = {};
    if (message.signer !== "") {
      obj.signer = message.signer;
    }
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.sessionHash.length !== 0) {
      obj.sessionHash = base64FromBytes(message.sessionHash);
    }
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.turnCount !== 0) {
      obj.turnCount = Math.round(message.turnCount);
    }
    if (message.tokenCount !== 0) {
      obj.tokenCount = Math.round(message.tokenCount);
    }
    if (message.providerType !== 0) {
      obj.providerType = providerTypeToJSON(message.providerType);
    }
    if (message.commitllmReceiptHash.length !== 0) {
      obj.commitllmReceiptHash = base64FromBytes(message.commitllmReceiptHash);
    }
    if (message.providerSignatureHash.length !== 0) {
      obj.providerSignatureHash = base64FromBytes(message.providerSignatureHash);
    }
    if (message.contributorId !== "") {
      obj.contributorId = message.contributorId;
    }
    if (message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgSubmitSessionAttestation>, I>>(base?: I): MsgSubmitSessionAttestation {
    return MsgSubmitSessionAttestation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgSubmitSessionAttestation>, I>>(object: I): MsgSubmitSessionAttestation {
    const message = createBaseMsgSubmitSessionAttestation();
    message.signer = object.signer ?? "";
    message.orgId = object.orgId ?? "";
    message.sessionHash = object.sessionHash ?? new Uint8Array(0);
    message.modelId = object.modelId ?? "";
    message.turnCount = object.turnCount ?? 0;
    message.tokenCount = object.tokenCount ?? 0;
    message.providerType = object.providerType ?? 0;
    message.commitllmReceiptHash = object.commitllmReceiptHash ?? new Uint8Array(0);
    message.providerSignatureHash = object.providerSignatureHash ?? new Uint8Array(0);
    message.contributorId = object.contributorId ?? "";
    message.epoch = object.epoch ?? "0";
    return message;
  },
};

function createBaseMsgSubmitSessionAttestationResponse(): MsgSubmitSessionAttestationResponse {
  return { accepted: false, verificationStatus: "" };
}

export const MsgSubmitSessionAttestationResponse: MessageFns<MsgSubmitSessionAttestationResponse> = {
  encode(message: MsgSubmitSessionAttestationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.verificationStatus !== "") {
      writer.uint32(18).string(message.verificationStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgSubmitSessionAttestationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSubmitSessionAttestationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.verificationStatus = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSubmitSessionAttestationResponse {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      verificationStatus: isSet(object.verificationStatus)
        ? globalThis.String(object.verificationStatus)
        : isSet(object.verification_status)
        ? globalThis.String(object.verification_status)
        : "",
    };
  },

  toJSON(message: MsgSubmitSessionAttestationResponse): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.verificationStatus !== "") {
      obj.verificationStatus = message.verificationStatus;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgSubmitSessionAttestationResponse>, I>>(
    base?: I,
  ): MsgSubmitSessionAttestationResponse {
    return MsgSubmitSessionAttestationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgSubmitSessionAttestationResponse>, I>>(
    object: I,
  ): MsgSubmitSessionAttestationResponse {
    const message = createBaseMsgSubmitSessionAttestationResponse();
    message.accepted = object.accepted ?? false;
    message.verificationStatus = object.verificationStatus ?? "";
    return message;
  },
};

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(base?: I): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(object: I): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(base?: I): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(_: I): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
