// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: wevibe/memory/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";
import {
  StoredMemoryCommitment,
  StoredMemoryRelationship,
  StoredPendingCommitment,
  StoredValidityMetadata,
} from "./state";

export const protobufPackage = "wevibe.memory.v1";

export interface QueryGetMemoryRequest {
  orgId: string;
  contentHash: Uint8Array;
}

export interface QueryGetMemoryResponse {
  memory?: StoredMemoryCommitment | undefined;
}

export interface QueryGetPendingCommitmentsRequest {
  orgId: string;
}

export interface QueryGetPendingCommitmentsResponse {
  commitments: StoredPendingCommitment[];
}

export interface QueryGetMemoryCountRequest {
  orgId: string;
}

export interface QueryGetMemoryCountResponse {
  count: string;
}

export interface QueryGetEpochMerkleRootRequest {
  orgId: string;
  epoch: string;
}

export interface QueryGetEpochMerkleRootResponse {
  merkleRoot: Uint8Array;
  memoryCount: string;
}

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  params?: Params | undefined;
}

export interface QueryListRelationshipsRequest {
  orgId: string;
  cid: string;
}

export interface QueryListRelationshipsResponse {
  relationships: StoredMemoryRelationship[];
}

export interface QueryGetValidityRequest {
  orgId: string;
  cid: string;
}

export interface QueryGetValidityResponse {
  metadata?: StoredValidityMetadata | undefined;
  found: boolean;
}

export interface QueryGetMemoriesBatchRequest {
  orgId: string;
  contentHashes: Uint8Array[];
}

export interface QueryGetMemoriesBatchResponse {
  memories: StoredMemoryCommitment[];
  notFound: Uint8Array[];
}

function createBaseQueryGetMemoryRequest(): QueryGetMemoryRequest {
  return { orgId: "", contentHash: new Uint8Array(0) };
}

export const QueryGetMemoryRequest: MessageFns<QueryGetMemoryRequest> = {
  encode(message: QueryGetMemoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    if (message.contentHash.length !== 0) {
      writer.uint32(18).bytes(message.contentHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetMemoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetMemoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contentHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetMemoryRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      contentHash: isSet(object.contentHash)
        ? bytesFromBase64(object.contentHash)
        : isSet(object.content_hash)
        ? bytesFromBase64(object.content_hash)
        : new Uint8Array(0),
    };
  },

  toJSON(message: QueryGetMemoryRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.contentHash.length !== 0) {
      obj.contentHash = base64FromBytes(message.contentHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetMemoryRequest>, I>>(base?: I): QueryGetMemoryRequest {
    return QueryGetMemoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetMemoryRequest>, I>>(object: I): QueryGetMemoryRequest {
    const message = createBaseQueryGetMemoryRequest();
    message.orgId = object.orgId ?? "";
    message.contentHash = object.contentHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseQueryGetMemoryResponse(): QueryGetMemoryResponse {
  return { memory: undefined };
}

export const QueryGetMemoryResponse: MessageFns<QueryGetMemoryResponse> = {
  encode(message: QueryGetMemoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.memory !== undefined) {
      StoredMemoryCommitment.encode(message.memory, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetMemoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetMemoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.memory = StoredMemoryCommitment.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetMemoryResponse {
    return { memory: isSet(object.memory) ? StoredMemoryCommitment.fromJSON(object.memory) : undefined };
  },

  toJSON(message: QueryGetMemoryResponse): unknown {
    const obj: any = {};
    if (message.memory !== undefined) {
      obj.memory = StoredMemoryCommitment.toJSON(message.memory);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetMemoryResponse>, I>>(base?: I): QueryGetMemoryResponse {
    return QueryGetMemoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetMemoryResponse>, I>>(object: I): QueryGetMemoryResponse {
    const message = createBaseQueryGetMemoryResponse();
    message.memory = (object.memory !== undefined && object.memory !== null)
      ? StoredMemoryCommitment.fromPartial(object.memory)
      : undefined;
    return message;
  },
};

function createBaseQueryGetPendingCommitmentsRequest(): QueryGetPendingCommitmentsRequest {
  return { orgId: "" };
}

export const QueryGetPendingCommitmentsRequest: MessageFns<QueryGetPendingCommitmentsRequest> = {
  encode(message: QueryGetPendingCommitmentsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetPendingCommitmentsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetPendingCommitmentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetPendingCommitmentsRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
    };
  },

  toJSON(message: QueryGetPendingCommitmentsRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetPendingCommitmentsRequest>, I>>(
    base?: I,
  ): QueryGetPendingCommitmentsRequest {
    return QueryGetPendingCommitmentsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetPendingCommitmentsRequest>, I>>(
    object: I,
  ): QueryGetPendingCommitmentsRequest {
    const message = createBaseQueryGetPendingCommitmentsRequest();
    message.orgId = object.orgId ?? "";
    return message;
  },
};

function createBaseQueryGetPendingCommitmentsResponse(): QueryGetPendingCommitmentsResponse {
  return { commitments: [] };
}

export const QueryGetPendingCommitmentsResponse: MessageFns<QueryGetPendingCommitmentsResponse> = {
  encode(message: QueryGetPendingCommitmentsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.commitments) {
      StoredPendingCommitment.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetPendingCommitmentsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetPendingCommitmentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commitments.push(StoredPendingCommitment.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetPendingCommitmentsResponse {
    return {
      commitments: globalThis.Array.isArray(object?.commitments)
        ? object.commitments.map((e: any) => StoredPendingCommitment.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryGetPendingCommitmentsResponse): unknown {
    const obj: any = {};
    if (message.commitments?.length) {
      obj.commitments = message.commitments.map((e) => StoredPendingCommitment.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetPendingCommitmentsResponse>, I>>(
    base?: I,
  ): QueryGetPendingCommitmentsResponse {
    return QueryGetPendingCommitmentsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetPendingCommitmentsResponse>, I>>(
    object: I,
  ): QueryGetPendingCommitmentsResponse {
    const message = createBaseQueryGetPendingCommitmentsResponse();
    message.commitments = object.commitments?.map((e) => StoredPendingCommitment.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryGetMemoryCountRequest(): QueryGetMemoryCountRequest {
  return { orgId: "" };
}

export const QueryGetMemoryCountRequest: MessageFns<QueryGetMemoryCountRequest> = {
  encode(message: QueryGetMemoryCountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetMemoryCountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetMemoryCountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetMemoryCountRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
    };
  },

  toJSON(message: QueryGetMemoryCountRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetMemoryCountRequest>, I>>(base?: I): QueryGetMemoryCountRequest {
    return QueryGetMemoryCountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetMemoryCountRequest>, I>>(object: I): QueryGetMemoryCountRequest {
    const message = createBaseQueryGetMemoryCountRequest();
    message.orgId = object.orgId ?? "";
    return message;
  },
};

function createBaseQueryGetMemoryCountResponse(): QueryGetMemoryCountResponse {
  return { count: "0" };
}

export const QueryGetMemoryCountResponse: MessageFns<QueryGetMemoryCountResponse> = {
  encode(message: QueryGetMemoryCountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.count !== "0") {
      writer.uint32(8).uint64(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetMemoryCountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetMemoryCountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.count = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetMemoryCountResponse {
    return { count: isSet(object.count) ? globalThis.String(object.count) : "0" };
  },

  toJSON(message: QueryGetMemoryCountResponse): unknown {
    const obj: any = {};
    if (message.count !== "0") {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetMemoryCountResponse>, I>>(base?: I): QueryGetMemoryCountResponse {
    return QueryGetMemoryCountResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetMemoryCountResponse>, I>>(object: I): QueryGetMemoryCountResponse {
    const message = createBaseQueryGetMemoryCountResponse();
    message.count = object.count ?? "0";
    return message;
  },
};

function createBaseQueryGetEpochMerkleRootRequest(): QueryGetEpochMerkleRootRequest {
  return { orgId: "", epoch: "0" };
}

export const QueryGetEpochMerkleRootRequest: MessageFns<QueryGetEpochMerkleRootRequest> = {
  encode(message: QueryGetEpochMerkleRootRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    if (message.epoch !== "0") {
      writer.uint32(16).uint64(message.epoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetEpochMerkleRootRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetEpochMerkleRootRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.epoch = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetEpochMerkleRootRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
    };
  },

  toJSON(message: QueryGetEpochMerkleRootRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetEpochMerkleRootRequest>, I>>(base?: I): QueryGetEpochMerkleRootRequest {
    return QueryGetEpochMerkleRootRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetEpochMerkleRootRequest>, I>>(
    object: I,
  ): QueryGetEpochMerkleRootRequest {
    const message = createBaseQueryGetEpochMerkleRootRequest();
    message.orgId = object.orgId ?? "";
    message.epoch = object.epoch ?? "0";
    return message;
  },
};

function createBaseQueryGetEpochMerkleRootResponse(): QueryGetEpochMerkleRootResponse {
  return { merkleRoot: new Uint8Array(0), memoryCount: "0" };
}

export const QueryGetEpochMerkleRootResponse: MessageFns<QueryGetEpochMerkleRootResponse> = {
  encode(message: QueryGetEpochMerkleRootResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.merkleRoot.length !== 0) {
      writer.uint32(10).bytes(message.merkleRoot);
    }
    if (message.memoryCount !== "0") {
      writer.uint32(16).uint64(message.memoryCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetEpochMerkleRootResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetEpochMerkleRootResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.merkleRoot = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.memoryCount = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetEpochMerkleRootResponse {
    return {
      merkleRoot: isSet(object.merkleRoot)
        ? bytesFromBase64(object.merkleRoot)
        : isSet(object.merkle_root)
        ? bytesFromBase64(object.merkle_root)
        : new Uint8Array(0),
      memoryCount: isSet(object.memoryCount)
        ? globalThis.String(object.memoryCount)
        : isSet(object.memory_count)
        ? globalThis.String(object.memory_count)
        : "0",
    };
  },

  toJSON(message: QueryGetEpochMerkleRootResponse): unknown {
    const obj: any = {};
    if (message.merkleRoot.length !== 0) {
      obj.merkleRoot = base64FromBytes(message.merkleRoot);
    }
    if (message.memoryCount !== "0") {
      obj.memoryCount = message.memoryCount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetEpochMerkleRootResponse>, I>>(base?: I): QueryGetEpochMerkleRootResponse {
    return QueryGetEpochMerkleRootResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetEpochMerkleRootResponse>, I>>(
    object: I,
  ): QueryGetEpochMerkleRootResponse {
    const message = createBaseQueryGetEpochMerkleRootResponse();
    message.merkleRoot = object.merkleRoot ?? new Uint8Array(0);
    message.memoryCount = object.memoryCount ?? "0";
    return message;
  },
};

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(base?: I): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(_: I): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(base?: I): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(object: I): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryListRelationshipsRequest(): QueryListRelationshipsRequest {
  return { orgId: "", cid: "" };
}

export const QueryListRelationshipsRequest: MessageFns<QueryListRelationshipsRequest> = {
  encode(message: QueryListRelationshipsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    if (message.cid !== "") {
      writer.uint32(18).string(message.cid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryListRelationshipsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListRelationshipsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListRelationshipsRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      cid: isSet(object.cid) ? globalThis.String(object.cid) : "",
    };
  },

  toJSON(message: QueryListRelationshipsRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.cid !== "") {
      obj.cid = message.cid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryListRelationshipsRequest>, I>>(base?: I): QueryListRelationshipsRequest {
    return QueryListRelationshipsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryListRelationshipsRequest>, I>>(
    object: I,
  ): QueryListRelationshipsRequest {
    const message = createBaseQueryListRelationshipsRequest();
    message.orgId = object.orgId ?? "";
    message.cid = object.cid ?? "";
    return message;
  },
};

function createBaseQueryListRelationshipsResponse(): QueryListRelationshipsResponse {
  return { relationships: [] };
}

export const QueryListRelationshipsResponse: MessageFns<QueryListRelationshipsResponse> = {
  encode(message: QueryListRelationshipsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.relationships) {
      StoredMemoryRelationship.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryListRelationshipsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListRelationshipsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.relationships.push(StoredMemoryRelationship.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListRelationshipsResponse {
    return {
      relationships: globalThis.Array.isArray(object?.relationships)
        ? object.relationships.map((e: any) => StoredMemoryRelationship.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryListRelationshipsResponse): unknown {
    const obj: any = {};
    if (message.relationships?.length) {
      obj.relationships = message.relationships.map((e) => StoredMemoryRelationship.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryListRelationshipsResponse>, I>>(base?: I): QueryListRelationshipsResponse {
    return QueryListRelationshipsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryListRelationshipsResponse>, I>>(
    object: I,
  ): QueryListRelationshipsResponse {
    const message = createBaseQueryListRelationshipsResponse();
    message.relationships = object.relationships?.map((e) => StoredMemoryRelationship.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryGetValidityRequest(): QueryGetValidityRequest {
  return { orgId: "", cid: "" };
}

export const QueryGetValidityRequest: MessageFns<QueryGetValidityRequest> = {
  encode(message: QueryGetValidityRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    if (message.cid !== "") {
      writer.uint32(18).string(message.cid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetValidityRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetValidityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetValidityRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      cid: isSet(object.cid) ? globalThis.String(object.cid) : "",
    };
  },

  toJSON(message: QueryGetValidityRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.cid !== "") {
      obj.cid = message.cid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetValidityRequest>, I>>(base?: I): QueryGetValidityRequest {
    return QueryGetValidityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetValidityRequest>, I>>(object: I): QueryGetValidityRequest {
    const message = createBaseQueryGetValidityRequest();
    message.orgId = object.orgId ?? "";
    message.cid = object.cid ?? "";
    return message;
  },
};

function createBaseQueryGetValidityResponse(): QueryGetValidityResponse {
  return { metadata: undefined, found: false };
}

export const QueryGetValidityResponse: MessageFns<QueryGetValidityResponse> = {
  encode(message: QueryGetValidityResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata !== undefined) {
      StoredValidityMetadata.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.found !== false) {
      writer.uint32(16).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetValidityResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetValidityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = StoredValidityMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetValidityResponse {
    return {
      metadata: isSet(object.metadata) ? StoredValidityMetadata.fromJSON(object.metadata) : undefined,
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
    };
  },

  toJSON(message: QueryGetValidityResponse): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = StoredValidityMetadata.toJSON(message.metadata);
    }
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetValidityResponse>, I>>(base?: I): QueryGetValidityResponse {
    return QueryGetValidityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetValidityResponse>, I>>(object: I): QueryGetValidityResponse {
    const message = createBaseQueryGetValidityResponse();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? StoredValidityMetadata.fromPartial(object.metadata)
      : undefined;
    message.found = object.found ?? false;
    return message;
  },
};

function createBaseQueryGetMemoriesBatchRequest(): QueryGetMemoriesBatchRequest {
  return { orgId: "", contentHashes: [] };
}

export const QueryGetMemoriesBatchRequest: MessageFns<QueryGetMemoriesBatchRequest> = {
  encode(message: QueryGetMemoriesBatchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    for (const v of message.contentHashes) {
      writer.uint32(18).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetMemoriesBatchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetMemoriesBatchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contentHashes.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetMemoriesBatchRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      contentHashes: globalThis.Array.isArray(object?.contentHashes)
        ? object.contentHashes.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.content_hashes)
        ? object.content_hashes.map((e: any) => bytesFromBase64(e))
        : [],
    };
  },

  toJSON(message: QueryGetMemoriesBatchRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.contentHashes?.length) {
      obj.contentHashes = message.contentHashes.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetMemoriesBatchRequest>, I>>(base?: I): QueryGetMemoriesBatchRequest {
    return QueryGetMemoriesBatchRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetMemoriesBatchRequest>, I>>(object: I): QueryGetMemoriesBatchRequest {
    const message = createBaseQueryGetMemoriesBatchRequest();
    message.orgId = object.orgId ?? "";
    message.contentHashes = object.contentHashes?.map((e) => e) || [];
    return message;
  },
};

function createBaseQueryGetMemoriesBatchResponse(): QueryGetMemoriesBatchResponse {
  return { memories: [], notFound: [] };
}

export const QueryGetMemoriesBatchResponse: MessageFns<QueryGetMemoriesBatchResponse> = {
  encode(message: QueryGetMemoriesBatchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.memories) {
      StoredMemoryCommitment.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.notFound) {
      writer.uint32(18).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetMemoriesBatchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetMemoriesBatchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.memories.push(StoredMemoryCommitment.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.notFound.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetMemoriesBatchResponse {
    return {
      memories: globalThis.Array.isArray(object?.memories)
        ? object.memories.map((e: any) => StoredMemoryCommitment.fromJSON(e))
        : [],
      notFound: globalThis.Array.isArray(object?.notFound)
        ? object.notFound.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.not_found)
        ? object.not_found.map((e: any) => bytesFromBase64(e))
        : [],
    };
  },

  toJSON(message: QueryGetMemoriesBatchResponse): unknown {
    const obj: any = {};
    if (message.memories?.length) {
      obj.memories = message.memories.map((e) => StoredMemoryCommitment.toJSON(e));
    }
    if (message.notFound?.length) {
      obj.notFound = message.notFound.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetMemoriesBatchResponse>, I>>(base?: I): QueryGetMemoriesBatchResponse {
    return QueryGetMemoriesBatchResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetMemoriesBatchResponse>, I>>(
    object: I,
  ): QueryGetMemoriesBatchResponse {
    const message = createBaseQueryGetMemoriesBatchResponse();
    message.memories = object.memories?.map((e) => StoredMemoryCommitment.fromPartial(e)) || [];
    message.notFound = object.notFound?.map((e) => e) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
