// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: wevibe/bandwidth/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";
import { StoredBandwidthOverride, StoredBandwidthState } from "./state";

export const protobufPackage = "wevibe.bandwidth.v1";

export interface QueryGetBandwidthStateRequest {
  orgId: string;
  epoch: string;
}

export interface QueryGetBandwidthStateResponse {
  state?: StoredBandwidthState | undefined;
}

export interface QueryGetBandwidthOverrideRequest {
  orgId: string;
}

export interface QueryGetBandwidthOverrideResponse {
  override?: StoredBandwidthOverride | undefined;
  hasOverride: boolean;
}

export interface QueryGetRemainingBandwidthRequest {
  orgId: string;
  epoch: string;
}

export interface QueryGetRemainingBandwidthResponse {
  memoryRemaining: string;
  serveRemaining: string;
}

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  params?: Params | undefined;
}

function createBaseQueryGetBandwidthStateRequest(): QueryGetBandwidthStateRequest {
  return { orgId: "", epoch: "0" };
}

export const QueryGetBandwidthStateRequest: MessageFns<QueryGetBandwidthStateRequest> = {
  encode(message: QueryGetBandwidthStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    if (message.epoch !== "0") {
      writer.uint32(16).uint64(message.epoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetBandwidthStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetBandwidthStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.epoch = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetBandwidthStateRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
    };
  },

  toJSON(message: QueryGetBandwidthStateRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetBandwidthStateRequest>, I>>(base?: I): QueryGetBandwidthStateRequest {
    return QueryGetBandwidthStateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetBandwidthStateRequest>, I>>(
    object: I,
  ): QueryGetBandwidthStateRequest {
    const message = createBaseQueryGetBandwidthStateRequest();
    message.orgId = object.orgId ?? "";
    message.epoch = object.epoch ?? "0";
    return message;
  },
};

function createBaseQueryGetBandwidthStateResponse(): QueryGetBandwidthStateResponse {
  return { state: undefined };
}

export const QueryGetBandwidthStateResponse: MessageFns<QueryGetBandwidthStateResponse> = {
  encode(message: QueryGetBandwidthStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== undefined) {
      StoredBandwidthState.encode(message.state, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetBandwidthStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetBandwidthStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.state = StoredBandwidthState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetBandwidthStateResponse {
    return { state: isSet(object.state) ? StoredBandwidthState.fromJSON(object.state) : undefined };
  },

  toJSON(message: QueryGetBandwidthStateResponse): unknown {
    const obj: any = {};
    if (message.state !== undefined) {
      obj.state = StoredBandwidthState.toJSON(message.state);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetBandwidthStateResponse>, I>>(base?: I): QueryGetBandwidthStateResponse {
    return QueryGetBandwidthStateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetBandwidthStateResponse>, I>>(
    object: I,
  ): QueryGetBandwidthStateResponse {
    const message = createBaseQueryGetBandwidthStateResponse();
    message.state = (object.state !== undefined && object.state !== null)
      ? StoredBandwidthState.fromPartial(object.state)
      : undefined;
    return message;
  },
};

function createBaseQueryGetBandwidthOverrideRequest(): QueryGetBandwidthOverrideRequest {
  return { orgId: "" };
}

export const QueryGetBandwidthOverrideRequest: MessageFns<QueryGetBandwidthOverrideRequest> = {
  encode(message: QueryGetBandwidthOverrideRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetBandwidthOverrideRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetBandwidthOverrideRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetBandwidthOverrideRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
    };
  },

  toJSON(message: QueryGetBandwidthOverrideRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetBandwidthOverrideRequest>, I>>(
    base?: I,
  ): QueryGetBandwidthOverrideRequest {
    return QueryGetBandwidthOverrideRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetBandwidthOverrideRequest>, I>>(
    object: I,
  ): QueryGetBandwidthOverrideRequest {
    const message = createBaseQueryGetBandwidthOverrideRequest();
    message.orgId = object.orgId ?? "";
    return message;
  },
};

function createBaseQueryGetBandwidthOverrideResponse(): QueryGetBandwidthOverrideResponse {
  return { override: undefined, hasOverride: false };
}

export const QueryGetBandwidthOverrideResponse: MessageFns<QueryGetBandwidthOverrideResponse> = {
  encode(message: QueryGetBandwidthOverrideResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.override !== undefined) {
      StoredBandwidthOverride.encode(message.override, writer.uint32(10).fork()).join();
    }
    if (message.hasOverride !== false) {
      writer.uint32(16).bool(message.hasOverride);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetBandwidthOverrideResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetBandwidthOverrideResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.override = StoredBandwidthOverride.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.hasOverride = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetBandwidthOverrideResponse {
    return {
      override: isSet(object.override) ? StoredBandwidthOverride.fromJSON(object.override) : undefined,
      hasOverride: isSet(object.hasOverride)
        ? globalThis.Boolean(object.hasOverride)
        : isSet(object.has_override)
        ? globalThis.Boolean(object.has_override)
        : false,
    };
  },

  toJSON(message: QueryGetBandwidthOverrideResponse): unknown {
    const obj: any = {};
    if (message.override !== undefined) {
      obj.override = StoredBandwidthOverride.toJSON(message.override);
    }
    if (message.hasOverride !== false) {
      obj.hasOverride = message.hasOverride;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetBandwidthOverrideResponse>, I>>(
    base?: I,
  ): QueryGetBandwidthOverrideResponse {
    return QueryGetBandwidthOverrideResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetBandwidthOverrideResponse>, I>>(
    object: I,
  ): QueryGetBandwidthOverrideResponse {
    const message = createBaseQueryGetBandwidthOverrideResponse();
    message.override = (object.override !== undefined && object.override !== null)
      ? StoredBandwidthOverride.fromPartial(object.override)
      : undefined;
    message.hasOverride = object.hasOverride ?? false;
    return message;
  },
};

function createBaseQueryGetRemainingBandwidthRequest(): QueryGetRemainingBandwidthRequest {
  return { orgId: "", epoch: "0" };
}

export const QueryGetRemainingBandwidthRequest: MessageFns<QueryGetRemainingBandwidthRequest> = {
  encode(message: QueryGetRemainingBandwidthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orgId !== "") {
      writer.uint32(10).string(message.orgId);
    }
    if (message.epoch !== "0") {
      writer.uint32(16).uint64(message.epoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetRemainingBandwidthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetRemainingBandwidthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.epoch = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetRemainingBandwidthRequest {
    return {
      orgId: isSet(object.orgId)
        ? globalThis.String(object.orgId)
        : isSet(object.org_id)
        ? globalThis.String(object.org_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
    };
  },

  toJSON(message: QueryGetRemainingBandwidthRequest): unknown {
    const obj: any = {};
    if (message.orgId !== "") {
      obj.orgId = message.orgId;
    }
    if (message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetRemainingBandwidthRequest>, I>>(
    base?: I,
  ): QueryGetRemainingBandwidthRequest {
    return QueryGetRemainingBandwidthRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetRemainingBandwidthRequest>, I>>(
    object: I,
  ): QueryGetRemainingBandwidthRequest {
    const message = createBaseQueryGetRemainingBandwidthRequest();
    message.orgId = object.orgId ?? "";
    message.epoch = object.epoch ?? "0";
    return message;
  },
};

function createBaseQueryGetRemainingBandwidthResponse(): QueryGetRemainingBandwidthResponse {
  return { memoryRemaining: "0", serveRemaining: "0" };
}

export const QueryGetRemainingBandwidthResponse: MessageFns<QueryGetRemainingBandwidthResponse> = {
  encode(message: QueryGetRemainingBandwidthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.memoryRemaining !== "0") {
      writer.uint32(8).uint64(message.memoryRemaining);
    }
    if (message.serveRemaining !== "0") {
      writer.uint32(16).uint64(message.serveRemaining);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetRemainingBandwidthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetRemainingBandwidthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.memoryRemaining = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.serveRemaining = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetRemainingBandwidthResponse {
    return {
      memoryRemaining: isSet(object.memoryRemaining)
        ? globalThis.String(object.memoryRemaining)
        : isSet(object.memory_remaining)
        ? globalThis.String(object.memory_remaining)
        : "0",
      serveRemaining: isSet(object.serveRemaining)
        ? globalThis.String(object.serveRemaining)
        : isSet(object.serve_remaining)
        ? globalThis.String(object.serve_remaining)
        : "0",
    };
  },

  toJSON(message: QueryGetRemainingBandwidthResponse): unknown {
    const obj: any = {};
    if (message.memoryRemaining !== "0") {
      obj.memoryRemaining = message.memoryRemaining;
    }
    if (message.serveRemaining !== "0") {
      obj.serveRemaining = message.serveRemaining;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetRemainingBandwidthResponse>, I>>(
    base?: I,
  ): QueryGetRemainingBandwidthResponse {
    return QueryGetRemainingBandwidthResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetRemainingBandwidthResponse>, I>>(
    object: I,
  ): QueryGetRemainingBandwidthResponse {
    const message = createBaseQueryGetRemainingBandwidthResponse();
    message.memoryRemaining = object.memoryRemaining ?? "0";
    message.serveRemaining = object.serveRemaining ?? "0";
    return message;
  },
};

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(base?: I): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(_: I): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(base?: I): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(object: I): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
