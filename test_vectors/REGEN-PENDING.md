# Test vectors pending regeneration

Per D-S28-1 (Bech32 HRP rename) and D-S28-2 (auth header rename),
the JSON test vectors in this directory contain values that were
textually substituted during CO-281. They must be regenerated
from the renamed chain (CO-282) and renamed SDK (CO-284) before
any cross-repo conformance test passes.
Vectors marked stale until CO-284 closes.
